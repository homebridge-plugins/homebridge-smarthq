{
  "watch": [
    "src",
    "config.schema.json"
  ],
  "ext": "ts, html, json",
  "ignore": [],
  "exec": "DEBUG= tsc && npm run plugin-ui && homebridge -T -D -P -I -U ~/.homebridge-dev .. ",
  "signal": "SIGTERM",
  "env": {
    "NODE_OPTIONS": "--trace-warnings"
  }
}
